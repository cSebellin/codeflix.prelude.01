/*module.exports = function snakeCase(str = "") {
    if (/(?!^)([A-Z])/g) {
        return str.replace(/(?!^)([A-Z])/g, ' $1')
            .replace(/[-\s]+(?=[a-zA-Z])/g, '_').toLowerCase()
    }
    else if (/[-\s]+(?=[a-zA-Z])/g) {
        return str.replace(/[-\s]+(?=[^A-Z])/g, '_').toLowerCase()
    }
}

let snakeCase = "";
if (str.match[: -/g])){
snakeCase = str.replace(/-/g, '-')
snakeCase = snakeCase.replace(/(^)-$                        
}
*/